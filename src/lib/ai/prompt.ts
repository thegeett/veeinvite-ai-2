// STUB — replaced by Stream B in Phase 4.
// See plan §9 for exact prompt structure and §5 for coherence instruction verbatim.

import type { Call2Input, Call3Input, ClassifierInput } from "@/lib/types";

export function buildCall2Prompt(_input: Call2Input): string {
  return "STUB — Stream B Phase 4 replaces this with the full Call 2 prompt per plan §9";
}

export function buildCall3Prompt(_input: Call3Input): string {
  return "STUB — Stream B Phase 4 replaces this with the full Call 3 prompt per plan §9";
}

export function buildClassifierPrompt(_input: ClassifierInput): string {
  return "STUB — Stream B Phase 4 replaces this with the classifier prompt per plan §12 and §30";
}
