// STUB — Stream C Phase 6. Publishes site. M1 sets is_published=true (no payment).

import { NextResponse } from "next/server";

export async function POST() {
  return NextResponse.json(
    { error: "Not implemented — Stream C Phase 6" },
    { status: 501 }
  );
}
